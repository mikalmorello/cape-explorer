## 1. Data

- [x] 1.1 Add `closed: true` to Provincetown Brewing Co., with the
      owner's own words as `notes`

## 2. Display

- [x] 2.1 Map popup shows a "Closed" indicator when `closed` is true
- [x] 2.2 List card shows a "Closed" indicator when `closed` is true

## 3. Specs

- [x] 3.1 Add the `closed` field to the `location-data` spec

## 4. Ship

- [x] 4.1 Validate, build, and manually verify the indicator renders
- [x] 4.2 Commit and push
- [x] 4.3 Archive this change
