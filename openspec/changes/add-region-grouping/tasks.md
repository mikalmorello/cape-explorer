## 1. Region lookup

- [x] 1.1 Create `src/lib/capeRegions.js`: town → region map covering
      all towns currently in `locations.json`, plus Nantucket/Martha's
      Vineyard towns for future entries; region display order;
      "Other" fallback for unmapped towns

## 2. List view grouping

- [x] 2.1 Group `LocationList` cards by region under headings, in
      geographic order, skipping empty groups

## 3. Verify & deploy

- [x] 3.1 Screenshot-verify locally, build passes
- [x] 3.2 Push to `main`, confirm deploy succeeds
